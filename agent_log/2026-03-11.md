# 2026-03-11

- Confirmed the repo did not explicitly set `codex exec` sandbox or approval flags; child sessions were inheriting Codex CLI defaults.
- Updated `src/codex.js` so Codex bot turns now invoke `codex exec` with explicit full-access flags, with the CLI parsing order fixed as `codex --ask-for-approval never exec --sandbox danger-full-access ...`.
- Updated `src/codex.test.js` expectations to cover the explicit approval and sandbox flags.
- Updated `README.md` to document that Codex child sessions run non-interactively with full local shell access.
- Added a generic `codex exec` approval-policy note to `MEMORY.md`.
- Verified with `pnpm test` under `Node v24.7.0`; the default shell `node` was `v16.13.0`, which is too old for this repo's `node --test` runner.
