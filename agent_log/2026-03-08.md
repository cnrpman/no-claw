# 2026-03-08

## 22:33 CST

- 初始化项目记录。
- 根据用户确认，当前仓库尚未开工。
- 产出 MVP spec: `/Users/jun/git/discord-codex/2026-03-08_22-33-24_spec.md`
- 本次收敛范围:
  - 普通 channel 中 `@codex` 仅使用当前用户命令创建新 thread 并回复
  - thread 中 `@codex` 继续同一会话
  - 暂不关注 token 预算、`compact`、整频道历史读取
- 发现 `Plan.md` 缺失，已在 spec 中记录，待项目 owner 后续补充

## 22:52 CST

- 开始实现最小可跑版本。
- 新增 Node.js 项目骨架:
  - `package.json`
  - `pnpm-lock.yaml`
  - `.env.example`
  - `README.md`
- 实现核心模块:
  - `src/index.js`: Discord bot 事件循环
  - `src/codex.js`: `codex exec` / `codex exec resume` 封装
  - `src/session-store.js`: 本地 JSON session store
  - `src/config.js`: 环境变量加载
  - `src/utils.js`: mention 清理、thread 命名、消息分片
- 实现行为:
  - 普通 channel 中 `@codex` 时创建 thread 并启动新 Codex 会话
  - 已登记 thread 中 `@codex` 时 resume 同一 Codex 会话
  - 仅使用当前用户消息作为 prompt，不读取整频道历史
  - 明确处理 thread 创建失败、resume 失败、session 丢失等错误
- 新增测试:
  - `src/utils.test.js`
  - `pnpm test` 通过
- 补充 `MEMORY.md`，记录 Codex CLI JSONL / resume 的可集成行为
- 额外验证:
  - 通过 Node wrapper 实测 `src/codex.js`
  - `createTurn()` 可拿到 `threadId`
  - `resumeTurn()` 可基于同一 `threadId` 继续返回结果

## 23:04 CST

- 调整运行时配置以贴合实际使用方式:
  - 移除环境变量 `CODEX_MODEL`
  - 模型选择改为用户在 Discord 消息内临时指定
  - 支持 `@codex --model <name> ...` 和 `@codex -m <name> ...`
- 调整 Codex 工作目录策略:
  - `CODEX_CWD` 默认改为 `./workspace`
  - 启动时若目录不存在则自动创建
- 更新文档:
  - `.env.example`
  - `README.md`
- 新增测试覆盖:
  - mention 命令中的 model flag 解析
  - 默认工作目录解析
- 验证结果:
  - `pnpm test` 通过
  - `node --check` 通过
  - 默认 `workspace/` 路径可解析并创建

## 23:46 CST

- 处理首次真实启动时看到的 `discord.js` 弃用告警
- 将 Discord client 事件监听从 `ready` 改为 `clientReady`
- 目的:
  - 保持与 `discord.js` v14/v15 兼容方向一致
  - 消除启动时的 deprecation warning

## 23:57 CST

- 将“幕后状态”暴露到 Discord thread 中
- 每次 Codex turn 现在会先输出一条状态卡，内容包括:
  - 新建还是 resume 会话
  - 本次真正发送给 Codex 的 Discord 上下文范围
  - `--model` 参数是否传入
  - `codex --json` 返回的 per-turn usage
- 保持实现边界明确:
  - 显示的是单次 turn usage
  - 不是 `/status` 那种账户级套餐用量
- 新增测试覆盖状态卡文案与 usage 格式化
- 验证结果:
  - `pnpm test` 通过
  - `node --check` 通过

## 00:08 CST

- 新增轻量请求确认反馈:
  - 每次收到有效 `@codex` 请求后，先给原消息加一个 `👀` reaction
  - reaction 失败只记录日志，不阻塞主流程
- 更新 README:
  - 新增 `Add Reactions` 权限要求
  - 说明若改用自定义表情，可能还需要 `Use External Emojis`

## 00:12 CST

- 将本地 `pnpm` 缓存目录 `.pnpm-store/` 加入 `.gitignore`
- 原因:
  - 避免把依赖缓存误提交进仓库

## 00:15 CST

- 精简 Discord thread 内的 Codex 状态卡文案
- 将较长的上下文说明压缩为:
  - 新会话: `current mention only`
  - resume: `current mention only; history via Codex session`
